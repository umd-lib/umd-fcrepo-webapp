<?xml version="1.0" encoding="UTF-8"?>

<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/web-app_5_0.xsd"
         version="5.0">

	<display-name>Fedora Repository</display-name>

  <context-param>
    <param-name>contextConfigLocation</param-name>
    <param-value>WEB-INF/classes/spring/repository.xml</param-value>
  </context-param>
  
  <context-param>
    <param-name>shiroEnvironmentClass</param-name>
    <param-value>org.apache.shiro.web.env.DefaultWebEnvironment</param-value>
  </context-param>
    
  <listener>
    <listener-class>org.apache.shiro.web.env.EnvironmentLoaderListener</listener-class>
  </listener>

  <!-- To allow request object to be wired to WebACAuthorizingRealm -->
  <listener>
    <listener-class>org.springframework.web.context.request.RequestContextListener</listener-class>
  </listener>

  <listener>
    <listener-class>org.fcrepo.http.commons.FedoraContextLoaderListener</listener-class>
  </listener>
  
  <listener>
    <listener-class>org.apereo.cas.client.session.SingleSignOutHttpSessionListener</listener-class>
  </listener>

  <servlet>
    <servlet-name>jersey-servlet</servlet-name>
    <servlet-class>org.glassfish.jersey.servlet.ServletContainer</servlet-class>
    <init-param>
      <param-name>jakarta.ws.rs.Application</param-name>
      <param-value>org.fcrepo.http.commons.FedoraApplication</param-value>
    </init-param>
    <load-on-startup>1</load-on-startup>
  </servlet>
  <servlet-mapping>
    <servlet-name>jersey-servlet</servlet-name>
    <url-pattern>/rest/*</url-pattern>
  </servlet-mapping>

  <servlet>
    <servlet-name>user-servlet</servlet-name>
    <servlet-class>edu.umd.lib.fcrepo.servlets.AuthnServlet</servlet-class>
  </servlet>
  <servlet-mapping>
    <servlet-name>user-servlet</servlet-name>
    <url-pattern>/user/*</url-pattern>
  </servlet-mapping>

  <servlet>
    <servlet-name>user-token-servlet</servlet-name>
    <servlet-class>edu.umd.lib.fcrepo.servlets.GenerateTokenServlet</servlet-class>
  </servlet>
  <servlet-mapping>
    <servlet-name>user-token-servlet</servlet-name>
    <url-pattern>/user/token</url-pattern>
  </servlet-mapping>

  <servlet>
    <servlet-name>user-logout-servlet</servlet-name>
    <servlet-class>edu.umd.lib.fcrepo.servlets.LogoutServlet</servlet-class>
  </servlet>
  <servlet-mapping>
    <servlet-name>user-logout-servlet</servlet-name>
    <url-pattern>/user/logout</url-pattern>
  </servlet-mapping>

  <!-- Custom Error Pages -->
  <error-page>
    <error-code>403</error-code>
    <location>/errors/403.jsp</location>
  </error-page>

  <!-- Filters and Filter Mapping Chain -->

  <!-- BEGIN UMD FILTERS -->
  <!-- The UMD filters need to occur before the stock fcrepo-webapp filters, since
       they need manipulate the incoming request *BEFORE* the WebACFilter in the
       fcrepo-webapp's Shiro filter chain makes its authorization determinations -->

  <filter>
    <filter-name>reverse-proxy-filter</filter-name>
    <filter-class>edu.umd.lib.fcrepo.filters.ReverseProxyFilter</filter-class>
  </filter>
  <filter-mapping>
    <filter-name>reverse-proxy-filter</filter-name>
    <url-pattern>/*</url-pattern>
  </filter-mapping>

  <filter>
    <filter-name>CAS Single Sign Out Filter</filter-name>
    <filter-class>org.apereo.cas.client.session.SingleSignOutFilter</filter-class>
  </filter>
  <filter-mapping>
    <filter-name>CAS Single Sign Out Filter</filter-name>
    <url-pattern>/*</url-pattern>
  </filter-mapping>

  <!-- using the Spring DelegatingFilterProxy allows us to configure the filters using environment variables -->
  <filter>
    <filter-name>CAS Authentication Filter</filter-name>
    <filter-class>org.springframework.web.filter.DelegatingFilterProxy</filter-class>
    <init-param>
      <param-name>targetBeanName</param-name>
      <param-value>casAuthenticationFilter</param-value>
    </init-param>
  </filter>
  <!-- only redirect to the login page for unauthenticated users in the /user URL space -->
  <filter-mapping>
    <filter-name>CAS Authentication Filter</filter-name>
    <url-pattern>/user/*</url-pattern>
  </filter-mapping>

  <filter>
    <filter-name>CAS Validation Filter</filter-name>
    <filter-class>org.springframework.web.filter.DelegatingFilterProxy</filter-class>
    <init-param>
      <param-name>targetBeanName</param-name>
      <param-value>casValidationFilter</param-value>
    </init-param>
  </filter>
  <filter-mapping>
    <filter-name>CAS Validation Filter</filter-name>
    <url-pattern>/*</url-pattern>
  </filter-mapping>

  <filter>
    <filter-name>CAS HttpServletRequest Wrapper Filter</filter-name>
    <filter-class>org.apereo.cas.client.util.HttpServletRequestWrapperFilter</filter-class>
  </filter>
  <filter-mapping>
    <filter-name>CAS HttpServletRequest Wrapper Filter</filter-name>
    <url-pattern>/*</url-pattern>
  </filter-mapping>

  <filter>
    <filter-name>Fedora Roles Filter</filter-name>
    <filter-class>edu.umd.lib.fcrepo.filters.FedoraRolesFilter</filter-class>
  </filter>
  <!-- look up user role on any request to the /user or /rest URL spaces -->
  <!-- we can omit the /static and / (root) spaces, since those are not access-restricted -->
  <filter-mapping>
    <filter-name>Fedora Roles Filter</filter-name>
    <url-pattern>/rest/*</url-pattern>
    <url-pattern>/user/*</url-pattern>
  </filter-mapping>

  <filter>
    <filter-name>JWT Bearer Token AuthNZ Filter</filter-name>
    <filter-class>edu.umd.lib.fcrepo.filters.TokenAuthnzFilter</filter-class>
  </filter>
  <filter-mapping>
    <filter-name>JWT Bearer Token AuthNZ Filter</filter-name>
    <url-pattern>/*</url-pattern>
  </filter-mapping>

  <!-- TODO: update the IP Mapper Filter to use IPManager -->
  <!-- filter>
    <filter-name>Fedora IP Mapper Filter</filter-name>
    <filter-class>org.springframework.web.filter.DelegatingFilterProxy</filter-class>
    <init-param>
      <param-name>targetBeanName</param-name>
      <param-value>ipMapperFilter</param-value>
    </init-param>
  </filter -->
  <!-- Map user's IP address to allowed categories on any request to the /user or /rest URL spaces -->
  <!-- we can omit the /static and / (root) spaces, since those are not access-restricted -->
  <!-- filter-mapping>
    <filter-name>Fedora IP Mapper Filter</filter-name>
    <url-pattern>/rest/*</url-pattern>
    <url-pattern>/user/*</url-pattern>
  </filter-mapping -->

  <!-- END UMD FILTERS -->

  <!-- The filter-name matches name of a 'shiroFilter' bean inside applicationContext.xml -->
  <filter>
    <filter-name>shiroFilter</filter-name>
    <filter-class>org.springframework.web.filter.DelegatingFilterProxy</filter-class>
    <init-param>
      <param-name>targetFilterLifecycle</param-name>
      <param-value>true</param-value>
    </init-param>
  </filter>
  <!-- Make sure any request you want accessible to Shiro is filtered. /* catches all -->
  <!-- requests.  Usually this filter mapping is defined first (before all others) to -->
  <!-- ensure that Shiro works in subsequent filters in the filter chain:             -->
  <!-- UMD Customization Note: Changed the url-pattern to only do Fedora authz on the -->
  <!-- repository contents (/rest/*), to allow the landing page and login page to be  -->
  <!-- accessed by unauthenticated users -->
  <filter-mapping>
      <filter-name>shiroFilter</filter-name>
      <url-pattern>/rest/*</url-pattern>
  </filter-mapping>

  <filter>
    <filter-name>repositoryInitializationFilter</filter-name>
    <filter-class>org.springframework.web.filter.DelegatingFilterProxy</filter-class>
  </filter>
  <filter-mapping>
    <filter-name>repositoryInitializationFilter</filter-name>
    <url-pattern>/rest/*</url-pattern>
  </filter-mapping>

  <filter>
    <filter-name>ETagFilter</filter-name>
    <filter-class>org.springframework.web.filter.ShallowEtagHeaderFilter</filter-class>
  </filter>
  <filter-mapping>
    <filter-name>ETagFilter</filter-name>
    <url-pattern>/static/*</url-pattern>
  </filter-mapping>

  <!-- Stops autoloading environment listeners on the context path -->
  <absolute-ordering/>
</web-app>
